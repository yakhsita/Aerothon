WSL Ubuntu
│
├── Gazebo (global)
├── ArduPilot (global)
│
└── venv (your_project_env)
    ├── opencv
    ├── dronekit
    ├── pymavlink
    └── your code
 _______________________________________
| WSL (Ubuntu 22/24):                   |
|   ArduPilot SITL  ←→  Gazebo (gz)     |      # Note1: not PX4 Ardupilot SITL      # Note2: PX4 can't work with Mission Planner, therefore use Ardupilot SITL
| Windows:                              |
|   Mission Planner (connects via UDP)  |
|_______________________________________|

# ubuntu commands
sudo apt update && sudo apt upgrade -y
sudo apt install git curl wget python3-pip python3-venv python3-dev build-essential lsb-release gnupg software-properties-common -y              #basic tools

    #installing gazebo gz-harmonic
sudo curl -fsSL https://packages.osrfoundation.org/gazebo.gpg -o /usr/share/keyrings/pkgs-osrf-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/pkgs-osrf-archive-keyring.gpg] http://packages.osrfoundation.org/gazebo/ubuntu-stable noble main" | sudo tee /etc/apt/sources.list.d/gazebo-stable.list > /dev/null
sudo apt update
sudo apt install gz-harmonic -y
    #testing gazebo
gz sim

    #installing ardupilot sitl
cd ~
git clone https://github.com/ArduPilot/ardupilot.git
cd ardupilot
Tools/environment_install/install-prereqs-ubuntu.sh -y       #really long process
. ~/.profile
cd ~/ardupilot
./waf configure --board sitl
./waf copter

    #running ardupilot sitl        (to stop sitl: Ctrl + C)
cd ~/ardupilot/ArduCopter
sim_vehicle.py -v ArduCopter --console --map
     








