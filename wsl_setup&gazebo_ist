WSL Ubuntu
│
├── Gazebo (global)
├── ArduPilot (global)
│
└── venv (your_project_env)
    ├── opencv
    ├── dronekit
    ├── pymavlink
    └── your code
 _______________________________________
| WSL (Ubuntu 22/24):                   |
|   ArduPilot SITL  ←→  Gazebo (gz)     |        # Note1: not PX4 Ardupilot SITL        # Note2: PX4 can't work with Mission Planner, therefore use Ardupilot SITL
| Windows:                              |
|   Mission Planner (connects via UDP)  |
|_______________________________________|

# install wsl (windows linux) in powershell (as administrator)
wsl --install
wsl -l -v         # states the name, state and version
wsl              # to run the Windows Linux
username: deeptirekha_hansdah + password: hansdah@DRH#348
echo $DISPLAY   # output is :0 (GUI ready -> for Gazebo)   # test ex: sudo apt install x11-apps -y
                                                                      xclock    # in-built clock gui
#wsl commands
sudo apt install -y python3 python3-pip python3-venv git   # basic dev tools (needed later)
note: Windows
      └── WSL Ubuntu (Linux environment)  ✓✓ done  # this is where -> gazebo,ardupilot,simulation will run
# powershell commands for installing 20.04 vers. (stable for gazebo)
wsl --install -d Ubuntu-20.04   # vers. (focal)
# wsl/ubuntu commands
 ㄨ(for gazebo-gazebo11 installation)
sudo apt update && sudo apt upgrade -y
sudo apt update
sudo apt install -y curl gnupg lsb-release
sudo curl -sSL https://packages.osrfoundation.org/gazebo.gpg | sudo tee /usr/share/keyrings/gazebo-archive-keyring.gpg > /dev/null
echo "deb [signed-by=/usr/share/keyrings/gazebo-archive-keyring.gpg] http://packages.osrfoundation.org/gazebo/ubuntu-stable $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/gazebo-stable.list
sudo apt update
sudo apt install gazebo11
gazebo        # to run the gazebo simulator
 ✓(for gazebo-gz vers. installation)

   (for ardupilot SITL installation)        


# powershell commands
python -m venv drone_env
drone_env\Scripts\activate

